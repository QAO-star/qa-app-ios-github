version: 2.1

# Use CircleCI's macOS executors for iOS builds
executors:
  macos:
    macos:
      xcode: "15.2.0"  # Latest stable Xcode version
  node:
    docker:
      - image: cimg/node:18.19

# Define reusable commands
commands:
  setup_node:
    description: "Setup Node.js environment"
    steps:
      - run:
          name: "Setup Node.js"
          command: |
            echo "📦 Setting up Node.js..."
            node --version
            npm --version
      
  install_dependencies:
    description: "Install project dependencies"
    steps:
      - run:
          name: "Install dependencies"
          command: |
            echo "📦 Installing dependencies..."
            npm install
            npm install -g @capacitor/cli
      
  setup_ios_platform:
    description: "Setup iOS platform with Capacitor"
    steps:
      - run:
          name: "Setup iOS platform"
          command: |
            echo "📱 Setting up iOS platform..."
            if [ -d "ios" ] && [ ! -f "ios/App/Podfile" ]; then
              echo "Removing incomplete iOS platform..."
              rm -rf ios
            fi
            if [ ! -d "ios/App/App.xcworkspace" ]; then
              echo "Adding iOS platform..."
              npx cap add ios
            fi
            echo "🔄 Syncing Capacitor..."
            npx cap sync ios
      
  setup_xcode:
    description: "Setup Xcode environment"
    steps:
      - run:
          name: "Setup Xcode"
          command: |
            echo "🔧 Setting up Xcode..."
            xcodebuild -version
            sudo xcode-select -s /Applications/Xcode.app/Contents/Developer
      
      - run:
          name: "Sign in to Apple Developer Account"
          command: |
            echo "🍎 Signing in to Apple Developer Account..."
            # Create a temporary keychain for signing
            security create-keychain -p "temp" build.keychain
            security default-keychain -s build.keychain
            security unlock-keychain -p "temp" build.keychain
            security set-keychain-settings -t 3600 -l ~/Library/Keychains/build.keychain
            
            # Sign in to Apple Developer account
            xcrun altool --store-password-in-keychain-item "apple-id" -u "jonatan.k@qaonline.co.il" -p "$APPLE_APP_SPECIFIC_PASSWORD" --keychain build.keychain || echo "⚠️ Apple ID sign-in failed, continuing..."
            
            echo "✅ Xcode setup completed"
      
  install_ios_dependencies:
    description: "Install iOS dependencies with CocoaPods"
    steps:
      - run:
          name: "Install iOS dependencies"
          command: |
            echo "📦 Installing iOS dependencies..."
            cd ios/App
            pod install --repo-update
      
  configure_signing:
    description: "Configure automatic signing for iOS builds"
    steps:
      - run:
          name: "Configure automatic signing"
          command: |
            echo "🔧 Configuring automatic signing..."
            cd ios/App
            
            # Ensure all projects use automatic signing
            sed -i '' 's/CODE_SIGN_STYLE = Manual;/CODE_SIGN_STYLE = Automatic;/g' App.xcodeproj/project.pbxproj
            sed -i '' 's/CODE_SIGN_STYLE = "Manual";/CODE_SIGN_STYLE = "Automatic";/g' App.xcodeproj/project.pbxproj
            sed -i '' 's/CODE_SIGNING_REQUIRED = NO;/CODE_SIGNING_REQUIRED = YES;/g' App.xcodeproj/project.pbxproj
            sed -i '' 's/CODE_SIGNING_ALLOWED = NO;/CODE_SIGNING_ALLOWED = YES;/g' App.xcodeproj/project.pbxproj
            
            # Configure Pods for automatic signing
            if [ -f "Pods/Pods.xcodeproj/project.pbxproj" ]; then
              echo "🔧 Configuring Pods for automatic signing..."
              sed -i '' '/PROVISIONING_PROFILE_SPECIFIER/d' Pods/Pods.xcodeproj/project.pbxproj
              sed -i '' 's/CODE_SIGN_STYLE = Manual;/CODE_SIGN_STYLE = Automatic;/g' Pods/Pods.xcodeproj/project.pbxproj
              sed -i '' 's/CODE_SIGN_STYLE = "Manual";/CODE_SIGN_STYLE = "Automatic";/g' Pods/Pods.xcodeproj/project.pbxproj
              sed -i '' '/CODE_SIGN_IDENTITY.*iPhone Distribution/d' Pods/Pods.xcodeproj/project.pbxproj
              sed -i '' 's/CODE_SIGN_IDENTITY = "iPhone Distribution";/CODE_SIGN_IDENTITY = "";/g' Pods/Pods.xcodeproj/project.pbxproj
            fi
            
            echo "✅ Automatic signing configured for all targets"
      
  setup_fastlane:
    description: "Setup Fastlane for certificate management"
    steps:
      - run:
          name: "Install Fastlane"
          command: |
            echo "💎 Installing Fastlane..."
            gem install fastlane
            echo "✅ Fastlane installed successfully"

# Define jobs
jobs:
  # Test build - runs on every push to main
  test_build:
    executor: node
    steps:
      - checkout
      - setup_node
      - install_dependencies
      - run:
          name: "Test web build"
          command: |
            echo "🔨 Testing web build..."
            npm run build
            echo "✅ Web build test completed successfully!"
      - store_artifacts:
          path: dist/
          destination: web-build

  # iOS test build - unsigned build for testing
  ios_test_build:
    executor: macos
    steps:
      - checkout
      - setup_node
      - install_dependencies
      - setup_ios_platform
      - setup_xcode
      - install_ios_dependencies
      - configure_signing
      - run:
          name: "iOS test build (unsigned)"
          command: |
            echo "🔨 Testing iOS build..."
            cd ios/App
            xcodebuild clean -workspace App.xcworkspace -scheme App -configuration Release
            xcodebuild build \
              -workspace App.xcworkspace \
              -scheme App \
              -configuration Release \
              -destination generic/platform=iOS \
              CODE_SIGNING_REQUIRED=NO \
              CODE_SIGNING_ALLOWED=NO
            echo "✅ iOS test build completed successfully!"
      - store_artifacts:
          path: ios/App/build/
          destination: ios-test-build

  # iOS App Store build - signed build for App Store
  ios_app_store_build:
    executor: macos
    steps:
      - checkout
      - setup_node
      - install_dependencies
      - setup_ios_platform
      - setup_xcode
      - install_ios_dependencies
      - configure_signing
      - setup_fastlane
      - run:
          name: "Test API key authentication"
          command: |
            cd ios/App
            fastlane test_api_key
      - run:
          name: "Test signing setup"
          command: |
            cd ios/App
            chmod +x test-signing-setup.sh
            ./test-signing-setup.sh
      
      - run:
          name: "Setup existing certificates and profiles"
          command: |
            cd ios/App
            echo "🔐 Setting up existing certificates and profiles..."
            
            # Install the existing provisioning profile from root
            if [ -f "../../QAOnlineAppStoreProfile.mobileprovision" ]; then
              echo "📄 Installing existing provisioning profile..."
              mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
              cp ../../QAOnlineAppStoreProfile.mobileprovision ~/Library/MobileDevice/Provisioning\ Profiles/
              echo "✅ Provisioning profile installed"
            else
              echo "❌ Provisioning profile not found at ../../QAOnlineAppStoreProfile.mobileprovision"
              exit 1
            fi
            
            # Try to generate certificates with Fastlane (optional)
            echo "🔐 Attempting to generate certificates with Fastlane..."
            fastlane certificates || echo "⚠️ Certificate generation failed, continuing with existing profile..."
            
            # Install any generated certificates if they exist
            if [ -f "certificates/distribution.cer" ]; then
              echo "🔐 Installing generated distribution certificate..."
              security import certificates/distribution.cer -k ~/Library/Keychains/login.keychain-db -T /usr/bin/codesign
            fi
            
            if [ -f "certificates/distribution.p12" ]; then
              echo "🔐 Installing generated P12 certificate..."
              security import certificates/distribution.p12 -k ~/Library/Keychains/login.keychain-db -T /usr/bin/codesign
            fi
            
            # Show available signing identities
            echo "🔍 Available signing identities:"
            security find-identity -v -p codesigning ~/Library/Keychains/login.keychain-db
            
            # Show installed provisioning profiles
            echo "📋 Installed provisioning profiles:"
            ls -la ~/Library/MobileDevice/Provisioning\ Profiles/ | grep -E "(QA-Online|\.mobileprovision)" || echo "No QA-Online profiles found"
      - run:
          name: "Build signed iOS app"
          command: |
            echo "🔨 Building signed iOS app..."
            cd ios/App
            
            # Make the signing script executable and run it
            chmod +x sign-and-export.sh
            ./sign-and-export.sh
      - run:
          name: "Upload to App Store Connect"
          command: |
            cd ios/App
            fastlane upload
      - store_artifacts:
          path: ios/App/App.ipa
          destination: ios-app-store-build
      - store_artifacts:
          path: ios/App/certificates/
          destination: certificates

  # Sign existing IPA - separate pipeline for signing only
  sign_existing_ipa:
    executor: macos
    steps:
      - checkout
      - setup_xcode
      
      - run:
          name: "Check for unsigned IPA"
          command: |
            echo "📥 Looking for unsigned IPA..."
            
            # Check if we have the IPA from a previous build
            if [ -f "ios/App/App.ipa" ]; then
              echo "✅ Found App.ipa from previous build"
              ls -la ios/App/App.ipa
              cp ios/App/App.ipa App.ipa
            else
              echo "❌ App.ipa not found from previous build"
              echo "📥 Please ensure the unsigned IPA is available"
              echo "🔗 You can download it from: https://app.circleci.com/pipelines/github/QAI-O/qa-app-ios"
              echo "📱 Look for the 'ios-app-store-build' artifact"
              echo ""
              echo "📋 To use this pipeline:"
              echo "1. Download the unsigned App.ipa from your previous build"
              echo "2. Place it in the ios/App/ directory"
              echo "3. Run this pipeline"
              exit 1
            fi
      
      - run:
          name: "Setup certificates and profiles"
          command: |
            echo "🔐 Setting up certificates and profiles..."
            
            # Install the existing provisioning profile
            if [ -f "QAOnlineAppStoreProfile.mobileprovision" ]; then
              echo "📄 Installing existing provisioning profile..."
              mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
              cp QAOnlineAppStoreProfile.mobileprovision ~/Library/MobileDevice/Provisioning\ Profiles/
              echo "✅ Provisioning profile installed"
            else
              echo "❌ Provisioning profile not found"
              echo "📥 Please ensure QAOnlineAppStoreProfile.mobileprovision is in the project root"
              exit 1
            fi
            
            # Install the P12 certificate with timeout and error handling
            if [ -f "ios_distribution.p12" ]; then
              echo "🔐 Installing P12 certificate..."
              
              # Try to install without password first
              timeout 60 security import ios_distribution.p12 -k ~/Library/Keychains/login.keychain-db -T /usr/bin/codesign -P "" || {
                echo "⚠️ First attempt failed, trying with different keychain..."
                # Try with system keychain
                timeout 60 security import ios_distribution.p12 -k /Library/Keychains/System.keychain -T /usr/bin/codesign -P "" || {
                  echo "⚠️ System keychain failed, trying login keychain..."
                  # Try with login keychain
                  timeout 60 security import ios_distribution.p12 -k ~/Library/Keychains/login.keychain -T /usr/bin/codesign -P "" || {
                    echo "❌ All certificate installation attempts failed"
                    echo "🔍 Checking if certificate is already installed..."
                    security find-identity -v -p codesigning ~/Library/Keychains/login.keychain-db | grep "iPhone Distribution" || {
                      echo "❌ No iPhone Distribution certificate found"
                      exit 1
                    }
                    echo "✅ Certificate appears to be already installed"
                  }
                }
              }
              echo "✅ P12 certificate installation completed"
            else
              echo "❌ P12 certificate not found"
              echo "📥 Please ensure ios_distribution.p12 is in the project root"
              exit 1
            fi
            
            # Show available signing identities
            echo "🔍 Available signing identities:"
            security find-identity -v -p codesigning ~/Library/Keychains/login.keychain-db
      
      - run:
          name: "Sign the IPA"
          command: |
            echo "🔐 Signing the IPA..."
            
            # Extract the IPA
            echo "📦 Extracting IPA..."
            unzip -q App.ipa -d Payload/
            
            # Sign the app bundle
            echo "🔐 Signing app bundle..."
            codesign --force --sign "iPhone Distribution" --entitlements QAOnlineAppStoreProfile.mobileprovision Payload/App.app
            
            # Verify the signature
            echo "🔍 Verifying signature..."
            codesign -dv --verbose=4 Payload/App.app
            
            # Recreate the signed IPA
            echo "📦 Creating signed IPA..."
            rm -f App.ipa
            zip -r App-signed.ipa Payload/
            
            # Clean up
            rm -rf Payload/
            
            echo "✅ IPA signed successfully!"
            ls -la App-signed.ipa
            echo "📊 File size: $(ls -la App-signed.ipa | awk '{print $5}')"
            echo ""
            echo "🎉 Successfully created signed IPA!"
            echo "📱 The signed IPA is ready for manual upload to App Store Connect"
      
      - store_artifacts:
          path: App-signed.ipa
          destination: signed-ipa

# Define workflows
workflows:
  version: 2
  
  # Release workflow - runs on tags only
  release:
    jobs:
      - ios_app_store_build:
          filters:
            tags:
              only: /^release-.*/
            branches:
              ignore: /.*/
  
  # Sign IPA workflow - runs on sign-ipa branch
  sign_ipa_workflow:
    jobs:
      - sign_existing_ipa:
          filters:
            branches:
              only: sign-ipa
